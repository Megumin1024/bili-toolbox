# 更新日志

本项目的所有重要变更记录在此文件中。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.0.4] - 2026-09-11

### 新增

- **用户动态**：输入 UID 或空间链接，抓取该用户的公开动态（图文 / 转发 / 投稿），可设置页数上限与页间隔。游客态即可使用，无需登录。
- **弹幕抓取**：输入视频链接抓取弹幕，可选择只抓指定分P或抓取全部分P，导出弹幕明细、密度分布 Excel 与原始 JSONL。游客态即可使用，无需登录。
- **弹幕分析**：在弹幕抓取结果上追加「弹幕分析」工作表与一份 Markdown 报告，包含热词、高频弹幕、时间轴热点分钟、弹幕模式/弹幕池构成和发送时间跨度。纯本地统计，不产生任何额外网络请求。

### 变更

- 弹幕工具统一使用 `core/text.py` 作为分词正本（中文 2/3/4 字滑窗 n-gram + 停用词 + 长词抑制），词频口径为「出现在多少条弹幕里」，同一条内的重复不累加。`tools/comments/core.py` 中的历史私有分词实现保持不变，迁移单独处理。

### 修复

- 修复网络层在无 GUI 场景（脚本 / CLI）下冷启动时的永久挂起。
- 修复用户动态任务在 HTTP 层退避等待期间无法响应「取消」的问题，此前最坏需等待 90 秒。
- **修复导出目录落在程序自身安装目录内的问题**：打包态此前把导出写到 exe 同级目录，重新打包（`--noconfirm` 会清空产物目录）时有连带删除用户导出文件的风险。现改为统一写入 `%USERPROFILE%\BiliToolbox\导出`，并在启动时把落在安装目录内的旧配置路径重新解析；用户自行选择的路径不受影响。

## [1.0.3] - 2026-09-10

### 修复

- 统一发布包、主窗口左下角和设置页“关于”区域的内部版本号。

## [1.0.2] - 2026-09-10

### 新增

- **任务预设**：评论抓取、视频采集和实时监控支持保存、应用及删除本地任务参数
- **视频采集对比看板**：多视频采集时展示总量、单视频变化、增速和互动率等对比数据
- **数据检查与修复**：检查本地 Excel / JSONL 文件，并在不覆盖源文件的前提下生成修复副本和修复清单
- **本地报告中心**：浏览已有评论结果、视频快照和监控历史，支持文件对比、时间段对比和 JSONL / XLSX 导出
- **监控提醒**：支持里程碑、增长停滞、异常突增、连续失败与恢复提醒，并提供 Windows 通知和声音提醒

### 变更

- 补充新功能对应页面、本地数据处理、测试与用户截图

## [1.0.1] - 2026-09-08

### 新增

- **任务历史**：记录评论抓取和视频采集的运行状态，支持查看详情、复用参数、打开结果和清理历史；持久化内容经过脱敏并保存在本机配置目录
- **运行诊断**：检查 Python、依赖、配置目录、输出目录和打包资源，支持查看详情与重新检查
- **最近错误**：保留最近一次可安全展示的错误记录，支持查看、复制和清理

### 变更

- 完成主窗口、设置页和标准任务页的深色 / 浅色主题及紧凑窗口适配，补充设置、诊断和任务历史界面
- 将任务状态、取消、限页中断和真实异常分开处理，保留断点续传与结果状态
- 补充架构文档、环境检查、边界检查、截图和自动化验证脚本

### 修复

- 修复评论抓取取消与重试退避期间的状态判断，避免主动取消被误报为运行异常
- 修复监控页面及相关展示在浏览器打开模式下的界面适配问题

## [1.0.0] - 2026-09-06

首个公开版本。

### 新增

- **评论抓取**：动态 / 视频链接 → gRPC 游客通道全量评论（主楼 + 楼中楼）→ 精确分析报告 → 多 sheet Excel；断点续传；-352 风控挑战人工验证后自动续跑
- **视频采集**：视频 / 收藏夹 / 合集 / 系列 / txt 列表批量快照与定时追踪，增量对比与增速榜 Excel；快照断点续传
- **实时监控**：单视频实时数据仪表盘（8 项指标 + 5 组趋势图 + 互动率分析），历史数据跨启动续接
- **统一网络层**：浏览器指纹身份、凭证预热与 buvid 激活、Chrome TLS 指纹双通道自动降级、代理池冷却轮换（设置页可配）
- **实验通道**：Chrome-TLS 指纹 gRPC（评论抓取页可选，失败自动回退标准 grpcio）
- **桌面应用**：PySide6 深色 / 浅色主题、任务进度 / 取消 / 日志 / 结果卡片、设置持久化
- **插件式扩展**：新增工具只需在 `tools/` 添加功能包并注册，自动获得完整 GUI 与网络层能力

### 变更

- 监控仪表盘改为自动在系统浏览器中打开（移除应用内嵌浏览器组件，安装包从约 400MB 减至约 45MB）

### 修复

- 部分视频「正在看」人数显示为空的解析问题（兼容 "1000+" 区间口径）

### 打包

- 单一 PyInstaller 配置（onedir，约 45MB）

[Unreleased]: https://github.com/Megumin1024/bili-toolbox/compare/v1.0.4...HEAD
[1.0.4]: https://github.com/Megumin1024/bili-toolbox/compare/v1.0.3...v1.0.4
[1.0.3]: https://github.com/Megumin1024/bili-toolbox/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/Megumin1024/bili-toolbox/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/Megumin1024/bili-toolbox/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Megumin1024/bili-toolbox/releases/tag/v1.0.0
